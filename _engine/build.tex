% -- Load build.lua before loading packages -- %
\directlua{dofile("_engine/build.lua")}
\input{_preamble/packages.tex}

\input{\directlua{tex.sprint('_preamble/' .. profile_name)}}

\usepackage[
    \directlua{tex.sprint(doc_papersize)},
    left=\directlua{tex.sprint(doc_left)},
    top=\directlua{tex.sprint(doc_top)},
    right=\directlua{tex.sprint(doc_right)},
    bottom=\directlua{tex.sprint(doc_bottom)}
]{geometry}

\directlua{SetFont()}

\usepackage[
    style=\directlua{tex.sprint(doc_bibstyle)},
    backend=biber,
    sorting=none,
    giveninits=true,
    uniquelist=false,
    uniquename=false,
    natbib=true,
    maxcitenames=1,
    mincitenames=1
]{biblatex}
\addbibresource{\directlua{tex.sprint(project.bibliography.file)}}

\usepackage{hyperref}
\hypersetup{
    colorlinks=\directlua{tex.sprint(doc_colorlinks)},
    linkcolor=\directlua{tex.sprint(doc_linkcolor)},
    urlcolor=\directlua{tex.sprint(doc_urlcolor)},
    citecolor=\directlua{tex.sprint(doc_citecolor)},
    pdfhighlight=\directlua{tex.sprint(doc_pdfhighlight)}
}
   
\hypersetup{
    pdftitle={\directlua{tex.sprint(doc_title)}},
    pdfauthor={\directlua{tex.sprint(doc_author)}}
}

\title{\directlua{tex.sprint(doc_title)}}
\author{\directlua{tex.sprint(doc_author)}}

\usepackage{cleveref}

\newcommand{\MakeFrontMatter}{
    \directlua{GenerateFrontMatter()}
}