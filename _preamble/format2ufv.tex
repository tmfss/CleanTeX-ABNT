\usepackage{setspace}
\onehalfspacing
\setmainfont{TeX Gyre Heros} % Carrega a fonte TeX Gyre Heros (clone da Arial)
\setsansfont{TeX Gyre Heros}
\renewcommand{\familydefault}{\sfdefault} % Mantém tudo em sans-serif
\iftoggle{numericformat}{
    \usepackage[
        backend=biber,
        style=abnt-numeric,
        justify,
        extrayear,
        giveninits=true,
        uniquelist=false,
        uniquename=false,
        natbib=true,
        maxcitenames=1,
        mincitenames=1
    ]{biblatex}
}{
    \usepackage[
        backend=biber,
        style=abnt,
        justify,
        extrayear,
        giveninits=true,
        uniquelist=false,
        uniquename=false,
        natbib=true,
        maxcitenames=1,
        mincitenames=1
    ]{biblatex}
}

\pagestyle{fancy}
\fancyhf{} 

%> O número da página no canto superior direito
\fancyhead[R]{\thepage} 
\iftoggle{useheaders}{
    %> Em caso de headers, colocamos o cabeçalho alinhado à esquerda
    \fancyhead[L]{\nouppercase{\leftmark}} % \nouppercase para evitar CAIXA ALTA
    \renewcommand{\headrulewidth}{0pt} % Sem linha para manter o padrão ABNT
}{
    \renewcommand{\headrulewidth}{0pt}
}

% Força as páginas de início de capítulo a manterem o número lá em cima
\fancypagestyle{plain}{
    \fancyhf{}
    \fancyhead[R]{\thepage}
    \renewcommand{\headrulewidth}{0pt}
}

%> Formatação dos capítulos enumerados alinhada à esquerda
\titleformat{\chapter}[block]
    {\normalfont\bfseries\filright}
    {\thechapter} % Numeração do capítulo em formato N
    {0.3cm} % Espaço em branco entre número e número do capítulo
    {\MakeUppercase} % Aplica uppercase ao nome do capítulo
    [\vspace{0.3cm}\titlerule] % Faz uma linha (rule) logo após o título

%> Formatação dos capítulos não-enumerados alinhada ao centro
\titleformat{name=\chapter,numberless}[block]
    {\normalfont\bfseries\filcenter}{}{0pt}
    {\MakeUppercase} % Aplica uppercase ao nome do capítulo
    [\vspace{0.3cm}\titlerule]

%> Configuração das epígrafes
\renewcommand{\epigraph}[2]{
  \begin{flushright}
  \begin{minipage}{.3\textwidth}
    \hspace{0.3cm}{#1}\vspace{0.3cm}\\
    {\itshape\small#2}
  \end{minipage}\hspace*{2cm}
\end{flushright}
\vspace{1.5cm}
}

